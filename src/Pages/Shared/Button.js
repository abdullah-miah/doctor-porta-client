import React from 'react';

const Button = () => {
    return (
        <div>
            <button class="btn btn-primary uppercase text-white text-bold bg-gradient-to-r from-secondary to-primary">
                Get Started
            </button>
        </div>
    );
};

export default Button;